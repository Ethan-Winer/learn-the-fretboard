import { NoteNumberToLetterPipe } from './note-number-to-letter-pipe';

describe('NoteNumberToLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new NoteNumberToLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
