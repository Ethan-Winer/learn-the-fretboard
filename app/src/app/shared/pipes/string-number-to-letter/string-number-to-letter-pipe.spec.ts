import { StringNumberToLetterPipe } from './string-number-to-letter-pipe';

describe('StringNumberToLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new StringNumberToLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
